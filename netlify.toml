# Netlify configuration. The official Next.js runtime is auto-installed by
# Netlify and handles server-side routes (so your API keys stay on the server).
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
